# Where the estate's guard implementations currently live.
#
# This one line is the whole point of the neutral address. The guards are still
# physically inside a vendor's directory today. When they move, this line moves
# with them, and nothing else on the machine changes: not the 45 repositories
# that inherit through core.hooksPath, not the launchd jobs, not any agent.
#
# An address the estate owns, pointing at a body the estate is still moving.
GUARD_IMPL="$HOME/.claude/scripts/hooks"
